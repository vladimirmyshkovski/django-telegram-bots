============
Installation
============

At the command line::

    $ easy_install telegram_bots

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv telegram_bots
    $ pip install telegram_bots
